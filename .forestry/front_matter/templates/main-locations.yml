---
label: 'Main Block: Locations'
hide_body: true
fields:
- name: locations
  type: field_group_list
  fields:
  - name: city
    type: text
    config:
      required: true
    label: City
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    default: "/.forestry/content/images/placeholder.png"
  - name: address
    type: textarea
    default: ''
    config:
      required: true
      wysiwyg: false
      schema:
        format: markdown
    label: Address
  - name: phone
    type: text
    config:
      required: true
      min: 10
      max: 10
    label: Phone
    description: Enter the 10-digit number here with no additional formatting.
  config:
    min: 
    max: 
    labelField: city
  label: Locations
