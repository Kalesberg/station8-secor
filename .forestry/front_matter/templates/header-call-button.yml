---
label: 'Header Block: Call Button'
hide_body: true
fields:
- name: phone
  type: text
  config:
    required: true
    min: 10
    max: 10
  label: Phone Number
  description: " Enter the 10-digit number with no extra formatting"
