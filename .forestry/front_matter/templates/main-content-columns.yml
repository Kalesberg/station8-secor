---
label: 'Main: Content Columns'
hide_body: true
display_field: title
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: columns
  type: field_group_list
  fields:
  - name: label
    type: text
    config:
      required: true
    label: Label
    description: Does not show for columns, but is the Label for the page tabs.
    default: Untitled
  - name: content
    type: include
    config: {}
    template: embeddable-blocks
    label: Content
  - name: settings
    type: include
    config: {}
    template: developer-settings
    label: Developer Settings
  config:
    min: 
    max: 
    labelField: label
  label: Columns
  description: Tabs will be hidden if only one is added and appear as side-by-side
    columns if only two are added.
- name: settings
  type: include
  config: {}
  template: developer-settings-main-blocks
  label: Developer Settings
