---
label: 'Header Block: Navigation'
hide_body: true
fields:
- name: links
  type: list
  config:
    use_select: true
    min: 
    max: 
    source:
      type: pages
      section: pages
  label: Links
