---
label: 'Main: Animated Image'
hide_body: true
fields:
- name: heading1
  type: text
  config:
    required: false
  label: Heading 1
- name: heading2
  type: text
  config:
    required: false
  label: Heading 2
- name: animatedElement
  type: select
  default: []
  config:
    required: false
    options:
    - Water
    - Eagle
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Animated Element
- name: backgrounds
  type: field_group_list
  fields:
  - name: class
    type: text
    config:
      required: false
    label: Custom Class Name
  - name: image
    type: include
    config: {}
    template: image
    label: Image
  config:
    min: 
    max: 
    labelField: title
  label: Background Layers
  description: Images should all be the same height and width. The <strong>first</strong>
    image in the list below will be the <strong>bottom</strong> layer and the the
    <strong>last</strong> image in the list will be the <strong>top</strong> layer.
- name: developerSettings
  type: include
  config: {}
  template: developer-settings-main-blocks
  label: Developer Settings
