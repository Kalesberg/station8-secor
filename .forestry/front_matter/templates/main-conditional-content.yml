---
label: 'Page Block: Conditional Content'
hide_body: true
fields:
- name: conditionalBlocks
  type: field_group_list
  fields:
  - name: state
    type: text
    config:
      required: true
    label: State
    description: The application state where this block will show.
  - name: block
    type: blocks
    label: Block
    template_types:
    - main-create-account
    config:
      min: 
      max: 
  config:
    min: 
    max: 
    labelField: 
  label: Conditional Blocks
