---
label: Slider
hide_body: true
fields:
- name: slides
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: true
    label: Title
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: text
    type: textarea
    default: ''
    config:
      required: true
      wysiwyg: false
      schema:
        format: markdown
    label: Text
  config:
    min: 1
    max: 
    labelField: title
  label: Slides
