---
label: 'Global Settings: Career'
hide_body: true
fields:
- name: ctaTitle
  type: text
  config:
    required: true
  label: CTA Title
- name: ctaText
  type: text
  config:
    required: true
  label: CTA Text
- name: background
  type: file
  config:
    maxSize: 64
  label: Background
- name: cfBackground
  type: file
  config:
    maxSize: 64
  label: Contact Form Background
